VERSIONING
----------
[major].[minor].[point]
 - Major releases see significant change to the feature set e.g. multiple minors.
 - Minor changes when at least one command is added, removed or changed, or a UI feature is added.
 - Point releases for bug fixes, UI modifications, meta and build changes.

**** Bugs ***********************
- Wrap states are restored (not on stringhorse either)

**** Features *************************
- after the | is for ...
  - empty 25
  - header ""
  - always (blank lines)

blank lines & always

- HTML <table> view of the output
- Nicer run button
- Icon
- Help tray
- comma detection in a single cell's output results in " escaping
1.0
- date func ... 
  - @date() puts in todays date
  - @date(start -10 step 1 skip 50) works like count: 0,0,0 if omitted
  - param for formats: short,long
- time func ...
  - @time() puts in the current time
  - @time(random) puts in a random time in the day
  - @time(hms|hm) formats
- 'order by' directive
- seed 0 as a directive to set a seed so that CSVs are random/deterministic
- search for redundant elements and get rid
- number of words parameter(s) for @lorem

**** IDEAS *************************************************************
- saved recipes - it's just a JSON file into localstorage
  - import/export state - keeps recipes and variables
- skipping numbers on a @count() sequence
- headers - declared in defn - can be blank
